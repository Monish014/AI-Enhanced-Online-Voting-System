// Re-export from context for ergonomic import:
// import useAuth from '@hooks/useAuth'  instead of  import { useAuth } from '@context/AuthContext'
export { useAuth as default } from '@context/AuthContext';
